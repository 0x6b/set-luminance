[package]
name = "akarusa"
version = "1.1.5"
edition = "2024"
description = "Controls luminance (brightness) of an external display over DDC"

[dependencies]

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = true
